import test from 'tape';
import {isEmail} from '../../../main/src/value-validation-functions';

// test('isEmail Tests', (assert) => {
//     assert.test('should return true when input is a valid email. (not meeting RFC 5322 standard, see code JsDoc))', (assert) => {
//     });
// });
